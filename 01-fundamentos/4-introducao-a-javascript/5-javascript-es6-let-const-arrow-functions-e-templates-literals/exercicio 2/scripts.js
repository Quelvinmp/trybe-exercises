// let status = 'desligado';

// const onOff = () => (
//     status.toLowerCase() === 'ligado' ? `O motor está ligado` : `O motor está desligado`
// );

// console.log(onOff());           // Requisito 1

// const pi = Math.PI;
// let raio = 5;
// const circleArea = (raio) => (`Essa é a área do círculo: ${pi * (raio**2)}`);

// console.log(circleArea(raio));          // Requisito 2


// let word = 'Antônio foi no banheiro e não sabemos o que aconteceu';
// let arrayWord = word.split(' ',);
// let biggestWord = arrayWord[0];

// // for (let index = 0; index < arrayWord.length; index += 1) {
// //     if (arrayWord[index].length > biggestWord.length) {
// //         biggestWord = arrayWord[index];
// //     }
// // }

// const longestWord = text => text.split(' ').sort((wordA, wordB) => wordB.length - wordA.length)[0];
// console.log(longestWord("Antonio foi ao banheiro e não sabemos o que aconteceu"));           // Requisito 3

